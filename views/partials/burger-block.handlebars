<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdev="{{devoured}}">
		{{#if devoured}}Already eaten!{{else}}Order Up!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>