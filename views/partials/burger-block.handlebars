<li>
	{{burger_name}}

	<button class="change-devoured" data-id="{{id}}" data-newdev="{{devoured}}">
		{{#if devoured}}Order again{{else}}Eat!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">Trash!</button>
</li>